
# From the Claude Week-1 Question

- **How do interpreters transform human written code into assembly language?
- **Does OS's Free List gets updated whenever we delete something or refresh?
- **'The Interpreter makes a *Map* in its head: `a` lives at `1005`.' - does it remember the memory address? If yes, how? If no, what happens?**
- **"The binary of `1005` tells the hardware: "Go to Bank 1, Row 5, Column 4." - something like that using Multiplexer." - How? I again forgot how multiplexer works.**